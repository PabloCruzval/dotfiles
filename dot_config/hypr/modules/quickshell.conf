# Quickshell border configuration
# Asymmetric gaps: border space on top/right/bottom, no extra gap on left

general {
    gaps_in = 0
    gaps_out = 10, 10, 10, 0
}

decoration {
    rounding = 25  # Match borderRounding
}
